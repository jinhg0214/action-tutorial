name: github checkout

on: [push]

jobs:
    build:
        runs-on: ubuntu-latest

        steps:
        - uses: actions/checkout@v2
        - name: Run pwd # 스텝의 이름 
        run : pwd
        - name: Run ls -al
        run : ls -al
      
  