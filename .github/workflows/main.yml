name: Hello World

on: [push] # 이벤트 타입 https://docs.github.com/en/rest/using-the-rest-api/github-event-types?apiVersion=2022-11-28

jobs:
  build:
  
    runs-on: ubuntu-latest
    
    steps:
    #- uses: actions/checkout@v2
    - name: Run pwd # 스텝의 이름 
      run : pwd
    - name: Run ls -al
      run : ls -al
    
